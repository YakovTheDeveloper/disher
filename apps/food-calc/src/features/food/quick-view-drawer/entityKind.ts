/**
 * Три сущности приложения — выбирает формулировку subtitle'а шапки дровера
 * («Информация о продукте/блюде»). Раньше определял также глиф-идентификатор
 * kicker'а — kicker снят 2026-07-23 (тип переехал в subtitle chrome-ряда).
 * Живёт ОТДЕЛЬНО от QuickViewDrawer.tsx: это не-компонентный модульный экспорт,
 * а файл-компонент под Fast Refresh обязан экспортировать только компоненты
 * (иначе HMR полностью гасит дровер при правке).
 */
export type QuickViewKind = 'product' | 'my-product' | 'dish';
