// TODO: migrate to Triplit — drawer system was removed during migration
// import { DrawerDefinition } from '@/types/common/drawer';

export interface ScheduleDrawerPayloads {
  // TODO: define drawer payloads with new drawer system
}

// TODO: migrate to Triplit — re-register drawers using drawerStore.show() pattern
export const scheduleDrawers: readonly any[] = [];
