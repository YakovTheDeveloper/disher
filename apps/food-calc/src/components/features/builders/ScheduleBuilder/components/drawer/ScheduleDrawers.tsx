import { DateChoose } from '@/components/features/builders/ScheduleBuilder/components/drawer/DateChoose';
import { DrawerDefinition, DrawerContext } from '@/types/common/drawer';

export interface ScheduleDrawerPayloads {
  // TODO: define drawer payloads with new drawer system
}

export const scheduleDrawers: readonly DrawerDefinition<any>[] = [
  // TODO: re-register drawers using drawerStore.show() pattern
];
