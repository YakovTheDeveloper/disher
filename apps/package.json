{
  "name": "disher-apps",
  "private": true,
  "scripts": {
    "dev": "concurrently -n back,front -c green,cyan --kill-others-on-fail \"cd disher-backend-3.0 && npm run dev:network\" \"cd food-calc && npm run dev-network\"",
    "dev:front": "cd food-calc && npm run dev-network"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "wait-on": "^9.0.4"
  }
}